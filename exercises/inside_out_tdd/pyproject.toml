[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wordle-cheat-python"
version = "0.1.0"
description = "Starter repo for the inside-out TDD wordle-cheat exercise"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
